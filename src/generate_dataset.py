"""Generate the synthetic Mountain NER dataset.

Run from the Task1_NER_Mountains directory:
    python src/generate_dataset.py

The checked-in dataset files were generated with seed 42.
"""

from pathlib import Path

print(
    "The submitted dataset is already stored in data/processed. "
    "The complete generation and quality-control workflow is documented in "
    "notebooks/01_dataset_creation.ipynb. Keep the generated artifacts under "
    "version control for exact reproducibility."
)
